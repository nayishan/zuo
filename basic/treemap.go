
import (
	t"
	rt"
)

func main() {
	 m = map[string]int{
		:         0,
		n":       1,
		          2,
		cript":   3,
		ng":      4,
		sophy":   5,
		ups":     6,
		ctivity": 7,
		          8,
		t":       9,
		          10,

	 keys []string
	 k := range m {
		 append(keys, k)

	t.Strings(keys)
	 _, k := range keys {
		intln("Key:", k, "Value:", m[k])

}

